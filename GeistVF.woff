@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --babson-green: #006747;
  --babson-dark: #004d35;
  --babson-light: #e8f5e9;
}

body {
  font-family: var(--font-geist-sans), system-ui, sans-serif;
}

@layer utilities {
  .text-balance {
    text-wrap: balance;
  }
}
